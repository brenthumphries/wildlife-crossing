---
title: "Log — 2026-07-30"
date: 2026-07-30
tags: [log, decision, build]
status: active
---

## What I worked on

**Pushed the three-day backlog queued since 2026-07-27, watched it go green in
CI, and confirmed B6 for real this time** — against the actual
exclude-filter/version-pin-fixed artifact, not the stale pre-fix one the
2026-07-29 session tested. Then wrote a reusable push runbook so the
commit → push → CI → artifact-verification loop doesn't have to be re-derived
by hand each session, and retired the one-off runbook it replaces.

## What got done

- **Cleared the stale `.git/index.lock`** (same recurring cause as
  2026-07-28 — the `weekly-build-review` harness) and pushed three thematic
  commits, `62a4a48` → `6e6efe4`:
  - `e54454e` — the orphaned 2026-07-27 daily log, never committed in the last
    push.
  - `260b5d6` — the full 2026-07-28 CI/export fix set: `.gitignore`,
    `ci.yml`, ADR 0012, `export-setup.md`, `export_presets.cfg`,
    `tools/check_pck_contents.py`, `tools/inspect_pck.py`,
    `tools/smoke_boot.sh`, the 2026-07-28 log, the amended build review, and
    the review index.
  - `6e6efe4` — the 2026-07-29 B5 decision and B3 fix: `roadmap.md`,
    `testing-setup.md`, `main.gd`, `infrastructure_manager.gd`,
    `simulation.gd`, three test files, and the 2026-07-29 log.
- **Watched CI run `30576303478` via `gh`** — both jobs green (`GUT tests`,
  `Export desktop builds`), both explicitly labeled
  `Godot 4.6.3-stable headless`. Closes the open question from the 07-28 log
  about whether the version pin actually holds on a runner: it does.
- **Verified the shipped pck contents** with `tools/check_pck_contents.py`
  against both the Linux and Windows artifacts: **77 files, pack format 3,
  built by Godot 4.6.3, all 20 data files present, zero `addons/gut` or
  `tests` paths.** Matches the 07-28 review's predicted shrink (370 → 77
  files, 1.64 MB → measured 166,232 bytes) almost exactly.
- **Relaunched the fresh macOS build.** This run's artifact upload preserved
  the `.app` bundle directly rather than zipping it, so no `xattr` quarantine
  step was needed this time. Windowed launch: tutorial loads, animals move,
  **B** builds the 2-tile span (not the whole segment — confirms ADR 0016 is
  live in the shipped build, not just in tests), **M** opens the world map
  with only Bow Valley unlocked. This is the first confirmation of B6's
  acceptance criteria against the *fixed* build — the 07-29 confirmation was
  against an artifact that predated the exclude-filter and version-pin
  fixes.
- **Authored [push-runbook.md](../../docs/push-runbook.md)** — a reusable
  runbook covering the full loop: stale-lock clearing, thematic commit
  staging, push, watching CI via `gh`, verifying pck contents, and
  relaunching/smoke-testing the result. Generalizes
  `docs/b1-commit-runbook.md`, which was scoped to a single one-off commit.
- **Deleted `docs/b1-commit-runbook.md`.** Its own header said "delete this
  file once B1 has landed"; it had been flagged as owed-and-still-untracked
  in three consecutive logs (07-27, 07-28, 07-29).

## Decisions made

- **`push-runbook.md` documents the full pipeline, not just git mechanics.**
  The commit/push steps are the easy part; the CI-watching and
  artifact-verification steps are what actually took iteration to get right
  this session (`gh` invocation syntax, `check_pck_contents.py`'s argument
  form, the zip-vs-bare-`.app` artifact-packaging difference), so those are
  the parts most worth having written down.
- **Commits stay grouped thematically, not one-per-day.** Continues the
  precedent set by the deleted B1 runbook and keeps `git log` reviewable per
  root `CLAUDE.md`'s Conventional Commits convention.

## Open questions / blockers

- **`push-runbook.md` and this log are themselves uncommitted** as of the end
  of this session — next terminal session should commit and push them, ideally
  as the runbook's own first dry run.
- **B4 (span placement UI) remains the only open build-review blocker.**
  Unblocked since 07-29 (its sole dependency, B3, is done).
- **C1 (visual cue + minimal HUD) remains open and independent of B4.**
- **V7 (cut v0.1.0 — release note + GitHub Release)** now has a real,
  verified B6 to build on, but per the 07-28 review it's worth waiting on B4
  and C1 so the tagged release is actually playable end to end, not just
  bootable.

## Next session

- **Commit and push `push-runbook.md` and this log** — see the runbook's own
  Steps 0–2.
- **B4 — span placement UI** (build mode, ghost preview, running cost). The
  only build-review blocker left.
- **C1 — visual cue + minimal HUD**, independent of B4.

---

## Related

- [[../build-reviews/2026-07-28-next-build]] — B6 now closed against the real,
  pushed build, not the stale one this amends
- [push-runbook](../../docs/push-runbook.md) — new; written this session
- [[2026-07-29]] — the prior, pre-fix B6 confirmation this session supersedes
- [testing-setup](../../docs/testing-setup.md) — `smoke_boot.sh` mechanics,
  referenced by the new runbook
- [ADR 0012](../../docs/adr/0012-godot-and-gut-version-pin.md) — the version
  pin confirmed holding in CI today
