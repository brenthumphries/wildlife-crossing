---
title: "Build Reviews"
date: 2026-07-06
tags: [build, review, index]
status: active
---

# Build Reviews

Weekly build-review notes produced by the `weekly-build-review` harness. Each
note answers one question: **what work is needed to reach the next working build
(or the first working build, if none exists yet)?**

Notes are named `YYYY-MM-DD-next-build.md`. Newest first.

## Index

<!-- Add one line per review, newest at top: -->
<!-- - [[YYYY-MM-DD-next-build]] — one-line headline (N blockers) -->

- [[2026-09-08-next-build]] — First build, measured at `16622ec` on **`main`**, `origin/main..HEAD` = **0**. **The week's only work is sitting in the working tree.** B8's two halves are both written — all seven credential patterns in `.gitignore`, and a signing-runbook **Part A pre-flight** that spells out the `git rm --cached` trap in full — and neither is committed. `commit-plan-2026-09-07.json` names the branch (`chore/credential-path`) and both commit bodies; `git branch --show-current` says `main`; **`daily-logs/2026-09-07.md` does not exist.** The session that did the work left no record of itself. And uncommitted is now *further* from `main` than it used to be: under ruleset `22403399` with no bypass actor it needs a branch, a PR and five green contexts, **friction no working session has yet paid cold**. **5 blockers, 9 core, 8 verification** — **B3 closed** (09-02 walk), the first blocker to leave since 08-29, and **V2 half closed** by `829e36b`. Suites: **24 scripts / 246 tests / 3,154 asserts**, 86 Python, both boots clean, sandbox export still fails on missing 4.6.3 templates. **A carried risk is retired**: 09-01's *"a failing export **skips** `smoke-windows`"* was made false by `667a847` — `if: ${{ !cancelled() }}` now sits on both **jobs** (`:143`, `:298`), and this is the fourth review that would otherwise have described a fixed defect. **Line-number drift, third consecutive review**: `ci.yml` 297 → **331**, every carried citation stale, and the 09-01 note prescribed "cite headings not lines" and then cited lines anyway. **One row promoted from cosmetic to load-bearing**: `ci.yml:294`'s wrong-scene job name is now duplicated verbatim as a required status check in `protect_main.json:56`, so renaming it alone yields a context that never reports and an *unmergeable* PR rather than a red one — two files, five literal strings, two carrying the Godot version, nothing testing that they match. **Two gates have never refused anything**: `dco` green on seven PRs and red on none, and the ruleset has admitted two and refused none. **C8 is part closed and now self-contradictory** — `pipeline-design.md` says the repo is public (never since when) while the committed `signing-runbook.md:137` still says it is *about to*; the correction is in the uncommitted diff. **Zero doc-drift rows closed**; pre-build-checklist is flagged for the eighth consecutive review. **V3 re-measured with its method stated** — 31 named / 23 missing / 74% — and recorded as *not comparable* to 09-01's 43/35/81%, whose extraction was never written down. **Vault backlog clean for the first time in four reviews**, which is exactly why the uncommitted code stands out. **Since amended (2026-09-09): B8 is closed** — both halves landed through PR #8 (`13730fe`, `a2b867b`), and the runbook's own `gh api --method PATCH` line was run for the first time, taking secret scanning and push protection from **disabled** to **enabled**, so the pre-flight is walked rather than only written. **Since amended again (2026-09-09b): this note shipped without its Step 6 audits, and running them late found sixteen defects.** The session ended between writing and auditing, and the note was committed in that gap. **The worst is a regression against a documented fix**: V3's *31 / 23 / 74%* is the number the 09-01 note explicitly recorded at `:1118-1120` as its own *"earlier, sloppier"* extraction bug — §11 names twelve tests as `file.gd::test_…` and a backtick-anchored regex drops all twelve — so this note re-derived a known-bad figure, published it, and labelled the correct **43 / 35 / 81%** unusable. Also wrong: 8 untested scripts not 6, `project.godot` 43 lines not 44, the zero-tests guard at `ci.yml:116-130` not `:118-131`, three PRs under the ruleset not two, and a §4 quote elided into a different claim. **C8 is closeable and nobody carried it through** — `a2b867b` put *"public since 2026-08-29"* into `signing-runbook.md:196-197`, meeting both clauses this note predicted B8 would meet. **And C8 was wrong about why**: `pipeline-design.md:397` *does* say since when and says **2026-09-05**, contradicting the 08-29 the log establishes, with `ci.yml:260` repeating it — two tracked files stating the project's threat-model premise, both wrong, which is a sharper form of the defect C8 exists to fix. **The completeness audit found four gaps**, the largest being that **nothing names how the Windows and Linux binaries reach `builds/`** before the manifest sweep — only the `.dmg` is built locally, `fetch_build.py` downloads run-scoped by design, and the runbook's own Part D forbids both workarounds, so `SHA256SUMS.txt` covers one platform of three and B7 has nothing to launch. Plus: no procedure anywhere cuts the tag or creates the Release (`gh release`/`git tag -a` → zero hits in `docs/`, `tools/`, `.github/`); `deploy-website.yml:57-65` will reject C6's copy because `keys.openpgp.org` is off its allowlist, on a job that is **not** a required context and so fails silently; and C1's scope omits `codesign=1 → 3`, without which the signature stays `adhoc` and B7 fails by construction. **ADR 0020 gates nothing for v0.1.0**, but adds `required_review_thread_resolution` to a merge gate the note describes as five checks and a click, and a third reason `protect_main.json` gets edited while still no item owns it. **Structural finding: no §4 row can ever close**, because §4 is record-only — which is why `pre-build-checklist.md` has survived eight reviews and needs to be a numbered item instead. **Since amended (2026-09-09): B8 is closed**, the first blocker to leave since B3 on 09-02. PR #8 merged as `6fbd34e`: `13730fe` puts all seven credential patterns in `.gitignore`, `a2b867b` puts the Part A pre-flight in `docs/signing-runbook.md`. `git check-ignore -v` returns seven lines against the committed file. Secret scanning and push protection were read **disabled**, set with the runbook's own `gh api --method PATCH` line, and re-read **enabled**, so the pre-flight is now walked rather than only written. **This note was corrected before it landed, not amended:** its §4 signing-runbook row cited `:452`, a line that only exists after the pre-flight lands, and gave the shift as +57 where the diff is +60. Nothing had been published to amend. **4 blockers, 9 core, 8 verification.** See [[../daily-logs/2026-09-09]].
- [[2026-09-01-next-build]] — First build, measured at `00574b8` on **`main`**, with `origin/main..HEAD` = **0** for the first time in a review. **The best week the project has had, and none of it happened this week:** B1a, B2, B5 and V9 all closed between 08-26 and 08-29 and are already in the 08-25 note's amendments, so this review closes nothing and `git log --since=2026-08-30` returns empty. Suites unchanged — 237/237 GUT, 86 Python, both headless boots clean, sandbox export still fails on missing 4.6.3 templates. **6 blockers, 9 core, 8 verification.** Three findings. **The top of the queue is finally clear**: B3 carried `Depends on: B2` through three reviews, B2 closed, and the artifact it needs should now live to ~2026-09-12 under `ci.yml:261`'s new 14-day branch instead of the two days that dominated 08-25 — *should*, because that is derived from a log, not read from GitHub. **The deadline moved rather than vanished**: whenever Apple approves the 08-27 enrolment, A4 delivers a `.p8` Apple permits one download of, into a repo whose `.gitignore` has **no credential patterns at all** and which the 08-29 log says has no push protection or secret scanning — nothing tracked that, so it is now **B8**, and unlike an artifact a leaked key is rotated, not regenerated. And **every `ci.yml` line citation carried from 08-25 is stale**, because `e5687cf` grew the file 279 → 297. **New: B8, C8** (write down that the repo is public — the only record is an untracked log) **and C9**, which is the one that matters: **Phase 2's fourth exit criterion is met in tests and unreachable in the artifact a stranger runs** (`main.gd:215`), and nine reviews have asserted "every exit criterion is met" without anyone disposing of it. **Two doc-drift rows left the table by being fixed** — the first time more than one has — and two arrived, including `export-setup.md:97` going stale again at the same line, from the very commit that closed V9. **This note's own first draft was a ship blocker.** The fact-integrity audit returned thirty-odd defects: four claims stated as fact that its own Unverifiable list contradicted, six greps whose conclusions held and whose stated evidence did not, and — in the finding whose subject was stale line numbers — **eight wrong line numbers**, derived by arithmetic instead of by opening the file. The completeness audit found C9 and was wrong once, proposing an item the 08-26 log had already closed. Both reports are in §Verification. **Since amended (2026-09-02): B3 is closed.** Run `33285150876` walked windowed on the Mac; the artifact's expiry is now measured rather than derived (`expired: false`, 2026-09-13T01:14:54Z), and the public API that gave it was recorded here as unreachable. Everything keyed passed, including the F5 / quit / relaunch / F9 round-trip that had never run from an exported binary, and `codesign` returned `adhoc,runtime` rather than the unsigned object the runbook predicts, which shrinks C1 to identity, team id and notarization. **The walk found what nine reviews of reading files could not:** the world map renders all twelve sub-areas locked, Bow Valley included, because `JSON.parse_string` yields floats and Godot dictionaries treat `7` and `7.0` as different keys, so `roadmap.md:116` is the **second** Phase 2 exit criterion met in tests and broken in the artifact. It is untracked and unnumbered, and should be a blocker. Also found: `game/project.godot` has no `[display]` section at all, so every UI string is too small to read at the default window size (recorded, not fixed, by Brent's call). See [[../daily-logs/2026-09-02]]. **Since amended again (2026-09-05): the float-key defect is closed.** Fixed at the data boundary in `species_registry.gd` `_index()` rather than at the two consumer lines, merged as `829e36b` from PR #2, and confirmed windowed against run `33973821586`: eleven cards padlocked, Bow Valley bright and outlined, every card named. `roadmap.md:116` is met by observation now. The suite reads real data through the registry instead of int-keyed fixtures and goes 23 scripts / 237 tests / 3,032 asserts to 24 / 246 / 3,154; backing the fix out fails 6 of them. The five-day vault backlog is committed in `c070b12`, which half closes C8 and part closes V2. `dco` ran for the first time since the 08-29 merge and was green, which does not advance V5. See [[../daily-logs/2026-09-05]]. **Since amended again (2026-09-06): `main` is protected.** Ruleset `22403399`, no bypass actor, requiring a pull request and all five CI contexts, with force pushes and deletion blocked. B8's branch-ruleset line is done and B8 is not: `.gitignore` still carries no credential patterns. PRs #5 (`0f14c6e`) and #6 (`82c991d`) are the first merges in project history to land 5 of 5, after `continue-on-error` on the artifact upload and `if: not cancelled()` on `export` and `smoke-windows` defused the `needs:` cascade that let PR #1 merge on a failed `export`. The Step 5 F1 ordering owed since 09-02 is fixed. New trap: `ci.yml:275`'s job name is now a required status check context, so the §4 row calling it cosmetic is load-bearing. See [[../daily-logs/2026-09-06]].
- [[2026-08-25-next-build]] — First build, measured at `4119915` — **the same commit as last week.** Nothing moved: zero commits in twelve days, and exactly three files in the repo have changed since 2026-08-18 (the previous review note, its index line — both still uncommitted — and `.obsidian/workspace.json`). The suites are byte-for-byte identical (237/237 GUT, 86 Python), both headless boots clean, and every item from [[2026-08-18-next-build]] was re-verified open by reading the same files. **The artifact the next step depends on expires around 2026-08-27**: `ci.yml:243` retains artifacts 14 days, run `31762062722` is from 2026-08-13, and the 08-13 log's next-session item #2 is literally `tools/fetch_build.py 31762062722 --check` — so merge PR #1 first (its `push: [main]` run yields a fresher and better artifact) or `gh run rerun`, since `ci.yml` has no `workflow_dispatch`. **Apple enrolment slipped a clean week for nothing** — thirteen days after it was first ranked, `grep -in fingerprint README.md` still returns nothing — and **two consecutive build reviews now live outside the repository**, with `origin/main` untouched for fifteen days and ten commits behind. **7 blockers, 7 core, 9 verification; numbers B1–B6/C1–C6/V1–V8 carried unchanged so the diff stays readable.** Three items are new, all from this review's audits: **B7** — walk the signing runbook's A8, download the published DMG on a clean Mac, *"No Gatekeeper warning at all"*, which is the only item that closes the **"and it launches"** half of the build case and which eight reviews left sitting in a reference list; **C7** — pin the release Mac's engine and install its 4.6.3 export templates, since the shipping `.dmg` is built locally by an engine no gate checks and `THIRD-PARTY-NOTICES.md:25` hard-codes the version; **V9** — add `workflow_dispatch` to `ci.yml`. **Closed: nothing.** **Three claims the repo carried are now falsified:** the 08-13 account of the stale `.git/index.lock` (mtime unmoved across two sessions of git commands — the correction is "cause unknown", not a third guess); the *"build-review V2 is still open"* claim in both `signing-runbook.md:392-395` and ADR 0018 §Follow-on (the three-platform pack gate and Windows boot landed 2026-08-10); and a **quotation invented two reviews ago** — 08-12 and 08-18 both attribute *"before the repo is announced publicly"* to `ci.yml:44-53`, and that phrase is not in the file. The fact-integrity audit returned 29 defects against this note's own first draft, 28 accepted; ten were wrong line numbers, several inherited and copied forward without re-opening the file. Both audit reports are recorded in §Verification. **Since amended (2026-08-26 and 2026-08-27):** B1's GPG half is closed (key `7F68A7E0...EFFC08FD`, all three publication channels done, fingerprint in `README.md`), and **B1b, the Apple enrolment, was submitted 2026-08-27** and is recorded in the repo for the first time; A2 and A4 now wait on Apple approval. B2's commit half landed and pushed (`c952a2c`, `ec06258`, `e7ebf9a`), so both outstanding reviews are inside the repository, but **B2 does not close**: PR #1 is unmerged and the merge must be a merge commit or rebase, since a squash would leave its acceptance reading 10. **V9 is written and uncommitted.** **New failure mode this note did not anticipate:** the `export` job hit the 500 MB Free artifact storage quota on 2026-08-26 and skipped `smoke-windows` through `needs: export`, which alone fails B2's acceptance; mitigated by a per-event `retention-days` split, a move to GitHub Pro, and B5, which removes Actions metering entirely on a public repo. **Since amended again (2026-08-29): B2 and B5 are closed.** PR #1 merged as `2e5f452`, a merge commit, and `origin/main..HEAD` reads 0 for the first time; the repository is `PUBLIC`, which also removed the Actions storage metering that had failed two export runs, and `harness/`, `tools/` and `.github/` are now in both structure tables. **Still open: B1b (with Apple since 2026-08-27), B3, B4, B6, B7, all of C, and V1 through V8.**
- [[2026-08-18-next-build]] — First build, measured at `4119915`. The best week the pipeline has had, though the best parts are *reported* rather than confirmed: [[../daily-logs/2026-08-13]] records CI running all five jobs green for the first time and a human launching a windowed macOS build of `HEAD`, and neither is checkable without GitHub access. Confirmed here: the Windows smoke gate fixed by allowlisting with 11 tests behind it, the push runbook corrected and mechanised into `tools/fetch_build.py`, tool tests 58 → 86, the GUT suite unchanged at 237/237 green. **And every item between a green pipeline and a downloadable release is exactly where it was six days ago.** Three findings: **the one calendar-bound task — Apple enrolment — has now been skipped by two consecutive next-session lists**, so it is ranked first this week rather than third; **`main` has not moved in eight days** (`origin/main..HEAD` = 10) while the 08-12 review, its index line and the 08-13 log have sat uncommitted for five days, so the document that says what blocks the build is not in the repo; and **nothing anywhere schedules making the repository public**, which three separate documents assume will happen and without which a Release is not downloadable at all. 6 blockers: GPG key + Apple enrolment, commit the vault files and merge PR #1, walk the windowed checklist, decide release packaging (the archive route, not `embed_pck` — the notice files have to travel with the binary), settle repo visibility, cut v0.1.0. Carried: 08-12's B5→B1, B2→B2, B3→B3, B4→B4, B6→B6, with C1–C6 and V1–V7 unchanged; **B5 and V8 are new.** **Closed: 08-12 B1** (`13857e0`), and the first doc-drift row ever to leave the table by being fixed (`export-setup.md:97`). Corrections to 08-12: the two runtime assets *are* guarded — by the smoke boot, not the pack gate — so that acceptance criterion drops to V8; and the `.dmg` does not break the CI pack gate, it bypasses it, because the shipped image is built on the Mac and gated by nothing.
- [[2026-08-12-next-build]] — First build, measured at `7e10b0c`; every Phase 1 and Phase 2 exit criterion is met in code and the suite is 237/237 green, **three of them have never been rendered to a human**, and **CI is broken on `HEAD` while `HEAD` has never been pushed**. `ci.yml:279` orphaned `retention-days: 14` into a `run:` block (exit 127 — `smoke-windows` fails on every run) and stripped it from the artifact upload; five commits sit unpushed, including the entire save/load and agent-respawn system. Two new gaps nothing had named: **`embed_pck=false` means a Release asset downloaded on its own is an engine with no game**, and every macOS signing key ADR 0018 requires is still at its default. 6 blockers: fix CI, push, export and verify `HEAD`, decide release packaging, start Apple enrolment + GPG, cut v0.1.0. Carried open from 08-04: B1→B3, B3→B6, C3→C5, V3–V6→V1–V4. **`7e10b0c` landed mid-review and closed two items an earlier draft had filed as deferrable** — agent respawn and save/load. **Since amended (2026-08-13):** B1 is closed, CI is green on all five jobs, and PR #1 carries ten commits. B2, B3 and V5 advanced without closing: `origin/main` has not moved, the windowed key-by-key checklist is unwalked, and the `dco` job has been seen green but never red. **Still open: B2 through B6, and the C and V lists.**
- [[2026-08-04-next-build]] — First build; B4 and C1 both landed and the suite reached 144/144 green, but **no build of `HEAD` has ever been launched or looked at** — CI likely exported one, nobody has run it, and the artifacts on disk fail the repo's own pck gate. Four core items (C2–C5) from the 07-28 review were dropped rather than closed, and a new defect: **the world-map click resolves against the world camera under an opaque card grid**, so map-screen clicking is blind in an export. 3 blockers: export and verify `HEAD`, make the scope call, cut v0.1.0. **Since amended:** B2 closed 2026-08-06 (scope call logged in `roadmap.md`; the blind click neutralized by `MOUSE_FILTER_STOP`), V1 closed, and **V2 and C4 closed 2026-08-10** (all three packs gated plus a Windows boot job; the tutorial measured, and it does demonstrate the mechanic). **Still open: B1, B3, C3, and V3 through V6.**
- [[2026-07-28-next-build]] — First build; the first binaries in project history exist. **Its B1 headline ("the pck ships zero game data") was later disproved** — see [[../daily-logs/2026-07-28]]; the pack was always complete and boots clean. **B2, B3, B4, B5, and B6 have since closed** (2026-07-28/29/30 logs) — a macOS `.app` has been launched and confirmed playable, the overpass follows ADR 0016's span geometry instead of paving whole segments, and the player now places spans directly (build mode, ghost preview, running cost) instead of a hardcoded default. **No build-review blockers remain open**; only C1 (visual cue + minimal HUD) stands before v0.1.0 (V7). **Correction (2026-08-06):** that last clause was wrong when written — this review's C2, C3, C4 and C5, and its V4, V5 and V6, were never closed either. They were re-verified open by [[2026-08-04-next-build]] and dispositioned by the 2026-08-06 scope decision (`docs/roadmap.md`, Phase 1 and Phase 2)
- [[2026-07-21-next-build]] — First build; last review's 7 blockers all closed and the suite doubled to 116/116 green, but the span defect, the missing visual cue, unimplemented hover highlight, 11 unreachable sub-areas, and zero binaries ever exported open 5 new ones
- [[2026-07-07-next-build]] — First build; no code change this week (HEAD still `5d0b5b9`, 57/57 green), harness now installed, same 7 blockers
- [[2026-07-06-next-build]] — First build; Phase 1 complete & 57/57 green, Phase 2 unstarted, no export pipeline (7 blockers)

## How to run

Invoke the `weekly-build-review` skill (Opus model) in this repo, or ask:
"Run the weekly-build-review for Wildlife Crossing." See
`harness/weekly-build-review/SKILL.md`.
