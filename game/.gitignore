# Godot 4 editor cache and import metadata
.godot/
*.import

# Exported binaries live in GitHub Releases, not the repo
/builds/

# GUT JUnit output (generated by the CI zero-tests guard)
/gut_results.xml
